const superHeros: string[]=[]
const heroPower: number[] = []
const heroPower: Array<number>=[]
// line 2 and 3 are different ways of representing same thing

type User = {
    name:string,
    isActive: boolean
}

const allUsers: User[]=[]
//array of type User
allUsers.push({name:"",isActive:true})

const MLModels: number[][]=[
    [255,244,233],
    []
]

// array which contains array of type numbers
superHeros.push("spiderman")
superHeros.push(2)



